input {
        http {
                port => 5044
        }
}
output {
        elasticsearch {
                hosts => "elasticsearch:9200"
                index => ["mpp-test-logs-%{+YYYY.MM.dd}"]

        }
}

